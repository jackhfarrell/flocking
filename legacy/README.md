# Legacy experiments

This directory keeps the earlier snapshot, streamline, correlator, calibration, and
single-temperature sweep scripts. They are historical reference material and are not part
of the maintained or tested production path.

The current velocity and temperature sweep is documented in the repository root and uses
`scripts/` together with `slurm/`.
